# Qwen3-VL-8B Requirements for RTX 4060 (8GB VRAM)
# Install with: pip install -r requirements.txt

# PyTorch with CUDA - INSTALL SEPARATELY FIRST:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# Core dependencies
transformers>=4.51.0
accelerate>=0.30.0
bitsandbytes>=0.45.0

# Qwen-VL specific
qwen-vl-utils
pillow>=10.0.0
numpy>=1.24.0

# Monitoring
pynvml

# Flask backend
flask>=3.0.0
flask-cors

# PDF processing
PyMuPDF

# Financial table detection (fuzzy title matching)
rapidfuzz>=3.5.0
